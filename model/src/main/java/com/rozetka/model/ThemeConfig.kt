package com.rozetka.model

enum class ColorConfig {
    DEFAULT,  // Динамические цвета (Monet от обоев)
    STANDARD, // Стандартная системная гамма (Material 3 Baseline)
    GREEN,
    BLUE,
    PURPLE,
    ORANGE
}

enum class ThemeConfig {
    SYSTEM,
    LIGHT,
    DARK
}
